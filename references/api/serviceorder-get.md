# GET /api/serviceorder/get

客户查询订单详情。

**前缀**：`/api/`（需要登录）

## 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int64 | 是 | 服务单 ID |

## 响应

`ServiceOrderDetail` 对象（结构见 `SKILL-CUSTOMER.md` 数据结构章节）。

## 用途

- 查看订单状态、服务者信息、金额、服务时间等
- 作为后续操作（修改时间、取消、验收）的前置信息查询
